#include <string>
#include <iostream>
#include <math.h>

using namespace std;

int main()
{
	int number;
	cin >> number;
	// long long result = pow(5, number);
	// solution 1: string based
	// string strResult = to_string(result);
	// cout << strResult.substr(strResult.length() - 2, 2) << endl;
	cout << 25;
	// solution 2: modular based
	// cout << result % 100 << endl;
}
